import React from 'react';
import styles from '../../styles.css';

const Featured = () => {

  return (
    <section id="featured" className={styles.featureSection}>
      <h2 className={styles.featuredText}>FEATURED RECIPES</h2>
      <div>
        <img />
      </div>
    </section>
  );
};

export default Featured;
